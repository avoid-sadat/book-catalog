export interface IProduct {
  
  _id: string;
  title: string;
  author: string;
  genre: string;
  published_date: string;
}

export interface Product {
  title: string;
  author: string;
  genre: string;
}